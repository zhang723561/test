promise 相关涉及内容 ： class类  this指向 event loop

class 类 ：
      构造函数 constructor 用于创建和初始化 class 类
        class通过 new 命令生成新的实例时会自动调用  如果没有显示定义 会默认添加一个
        空的constructor 方法

      对于派生的类 必须先调用super() 才能继续使用 this

      类的所有实例会共享一个原型对象：__proto__属性相等 可以利用这个属性为类添加方法
        使用Object.getPrototypeOf 方法来获取实例对象的原型，然后再来为原型添加方法/属性



